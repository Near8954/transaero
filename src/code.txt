
main {


    func(a, b, c);

    print(s, v, o);



    a = b + c;

    array a[b];

    array c[7];

    switch (7) {
        case ("zalupa") {
            "potnie mydya";
        }
        case ("chlen") {
            "nu i hueta";
        }
        case ("chlen") {
                    "nu i hueta";
        }
        case ("yabloki") {a + b -c+d;}
    };

}