#function int hui(int z) {empty;}

function float zzz(empty) {int a = b;}#


main {
    switch (a) {
        case ("zalupa") {
            "potnie mydya";
        }
        case ("chlen") {
            "nu i hueta";
        }
        case ("chlen") {
                    "nu i hueta";
        }
        case ("yabloki") {a + b -c+d;}
    };
}